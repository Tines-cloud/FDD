Compare these two FHIR profiles and detect EVERY semantic drift between them.

YOU ARE THE PRIMARY DETECTOR AND THE FINAL FILTER.
- The seeds below are HINTS from a deterministic comparator known to over-report.
- Your emitted JSON array IS the final drift report. Anything you omit is treated as
  REJECTED and will NOT be added back downstream.
- Confirm real seeds by re-emitting them, refine where needed, REJECT (omit) seeds that
  are inherited base noise or low-value conformance markers (see system prompt).
- Add every drift the seeds missed.

PROFILE CONTEXT (full normalised data for both profiles):

{{profileContext}}

RULE-BASED PRE-ANALYSIS (SEED ITEMS - HINTS ONLY, NOT GROUND TRUTH):

{{ruleBasedSeeds}}

INSTRUCTIONS

1. Use the `source` and `target` blocks of the profile context to compare elements,
   slices, extensions, bindings, cardinalities, types, fixed/pattern values, and
   constraints SLICE-BY-SLICE.

2. For each seed item:
   - If you agree it is a true profile-level semantic drift, RE-EMIT it with the same
     `type`, `sourcePath`, and `targetPath` (it will be marked "hybrid" downstream).
     You may improve `description` and `severity`.
   - If you disagree (the seed is wrong, irrelevant, or inherited base noise), OMIT it.

3. Now perform your own independent analysis and emit drift items for EVERY semantic
   difference you find that is NOT in the seed list - including:
   - Slices added or removed (use `path:sliceName` notation, never bare path).
   - Extension differences in URL, type, internal content, or cardinality.
   - Binding value-set changes, additional bindings, max value sets, strength shifts.
   - Type code changes (including one-sided: when one profile has a type the other lacks).
   - Constraint additions, removals, or expression changes.
   - Fixed/pattern value differences on identifier system, Reference targetProfile,
     boolean, or coded elements.
   - Cardinality changes (min or max).
   - Cross-version (R4 vs R5) structural differences.

4. Skip pure noise: isSummary changes, untouched inherited base elements, snapshot
   technical scaffolding.

5. Prefer ELEMENT/SLICE-level granularity. Do not emit one drift per sub-element when
   one parent-level item conveys the same information.

6. Use slice notation in paths whenever a sliceName exists
   (e.g. `Organization.identifier:hpio`, `Patient.extension:employeeId`).

7. Severity: ERROR for breakage/data loss; WARNING for interoperability impact;
   INFO for cosmetic-only.

OUTPUT
Return ONLY a JSON array of drift items - no markdown, no code fences, no prose.
The array must include both your re-emitted seeds (those you agreed with) AND your
own newly-detected drifts. Use ids `ai-1`, `ai-2`, ... for new items.
